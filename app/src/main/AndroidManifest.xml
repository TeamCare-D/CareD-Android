<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.caredirection.cadi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.caredirection.cadi.MainActivity">

        </activity>
        <activity android:name=".product.list.ListActivity">


        </activity>
        <activity android:name=".product.search.ProductSearch">
        <activity android:name=".research.ResearchInterestActivity"></activity>
        <activity android:name=".research.ResearchMedicineActivity" />
        <activity android:name=".research.ResearchDiseaseActivity" />
        <activity android:name=".research.ResearchGenderActivity" />
        <activity android:name=".research.ResearchNicknameActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>